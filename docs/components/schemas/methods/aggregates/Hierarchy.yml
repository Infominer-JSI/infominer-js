title: "hierarchy"
allOf:
- $ref: "Basic.yml"
- description: "The hierarchy statistics generated out of `category` field types."
  properties:
    type:
      default: "hierarchy"
    statistics:
      properties:
        values:
          allOf:
            - description: "The hierarchy starting with the root element."
            - $ref: "HierarchyItem.yml"
